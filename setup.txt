AWS

# heroku
heroku pg:reset DATABASE_URL
heroku run bundle exec rails db:migrate
heroku run bundle exec rails db:seed

# reset database
bundle exec rails db:drop
bundle exec rails db:create
bundle exec rails db:schema:load
bundle exec rails db:seed

# fonts
font-family: 'Programme', sans-serif;
font-family: 'Programme Regular', sans-serif;
font-family: 'Programme Light', sans-serif;
font-family: 'Barlow Condensed', sans-serif;
font-family: 'Ubuntu Condensed', sans-serif;
font-family: 'Raleway', sans-serif;

issues:
--- graphql ---
resolvers for helper functions for session queries?
application_controller helper_method for login!/logout! ?
make definition for login/logout in user and session controllers?
figure out how have graphql queryies dispatch to redux store - done
change types and figure out renaming process and do rest of actions

--- testing ---
use msw for fetches?
test action calls in components
test actions/reducers/store/entry/etc.
test currentUser/no currentUser

redis
firebase
microsoft azure
react native
Cognito
dynamodb
opensearch/elasticsearch